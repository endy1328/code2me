# 실제 프로젝트 검증 게이트

## 목적

- 합성 fixture 중심 회귀만으로는 놓칠 수 있는 실제 레거시 구조를 검증한다.
- 하지만 일상 개발 속도를 해치지 않도록, 실제 프로젝트 검증은 `release gate` 또는 `정기 수동 점검`으로 분리한다.

## 기본 원칙

- 일상 회귀의 1순위는 `합성 fixture`다.
- 실제 프로젝트 검증은 `정확도 확인용 품질 게이트`다.
- 실제 프로젝트 입력은 가능하면 `read-only snapshot` 또는 `익명화된 구조`로 받는다.
- 민감 정보 제거 과정에서 `class / bean / mapper / route` 연결 신호가 사라지면 검증 가치가 떨어지므로, 익명화 정책은 보수적으로 설계한다.

## 권장 입력 형태

### 1. 1순위

- 익명화된 실제 프로젝트의 `snapshot.json`
- 익명화된 입력 프로젝트 트리
- 최소 실행 로그

### 2. 2순위

- 공개 가능한 구조 유사 샘플
- 문제가 재현되는 축만 축소한 fixture

### 3. 지양

- 권한 제약 때문에 반복 검증이 어려운 원본 사내 저장소를 일상 회귀에 직접 연결하는 방식

## 검증 항목

- profile detection이 기대 범위 안에 들어오는가
- 주요 `web.xml` entry와 dispatcher가 보존되는가
- 대표 screen flow가 최소 1개 이상 설명 가능한가
- 대표 non-screen flow가 최소 1개 이상 분류 가능한가
- controller/service/dao 연결이 과도한 false positive 없이 유지되는가
- mapper/sql 또는 integration path가 과장 없이 유지되는가
- `heuristic` 카드가 main flow를 대체하지 않는가
- `report.html`과 내부 미러 산출물이 정상 생성되는가

## 판정 방식

### Pass

- 핵심 진입 흐름과 대표 business path가 사람 눈으로 추적 가능하다.
- 강한 근거가 있는 flow가 유지된다.
- 약한 추정은 보조 카드로 남는다.

### Soft Fail

- 주요 flow는 보이지만 route 선택, layout 연결, SQL fallback 등에서 일부 오탐/누락이 있다.
- 합성 fixture로 재현 가능한 축으로 환원해서 후속 작업으로 보낸다.

### Hard Fail

- 대표 entry가 누락된다.
- screen/api가 뒤섞인다.
- direct evidence보다 fallback이 앞선다.
- 내부 미러나 report generation이 깨진다.

## 운영 절차

1. 실제 프로젝트 또는 익명화 snapshot을 확보한다.
2. `npm run analyze -- <project-root>` 또는 snapshot 비교 절차를 수행한다.
3. 핵심 flow 3개를 수동 확인한다.
4. 실패 패턴을 `합성 fixture`로 환원한다.
5. 회귀 테스트를 추가한 뒤 실제 프로젝트 게이트를 다시 확인한다.

## 실패 패턴 기록 템플릿

- 프로젝트 유형:
- 대표 entry:
- 기대 flow:
- 실제 결과:
- 실패 분류:
  - entry
  - mapping
  - persistence
  - view/layout
  - inference/confidence
  - report rendering
- 합성 fixture 환원 가능 여부:
- 후속 조치:

## 최근 공개 샘플 게이트 기록

### 2026-04-13: `mybatis/jpetstore-6`

- 입력 형태:
  - 공개 GitHub 샘플 프로젝트 clone
  - `web.xml`, `applicationContext.xml`, JSP 포함
- 실행:
  - `npm run analyze -- /tmp/jpetstore-6-code2me`
- 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 9` on 2026-04-13
  - `entryPointCount = 1`
  - `warningCount = 0`
  - `StripesDispatcher (*.action)` entry는 보존
  - `screenFlowCards`는 생성되지만 대부분 `view-only` fallback
  - 대표 action handler / business path 복원은 아직 약함
- 판정:
  - `Soft Fail`
- 실패 분류:
  - `mapping`
  - `view/layout`
- 해석:
  - `legacy-java-ee` 경계 문제는 해소됐고, 이제는 `action-family-legacy-web`가 실제로 선택된다.
  - 다만 JPetStore 6 수준의 Stripes 앱에서는 action handler와 JSP 연결이 아직 충분히 강하지 않다.
- 후속 조치:
  - Stripes real-world 샘플용 `view-to-action reverse linkage` 보강
  - `ActionBeanContext` / validation / forward chain 해석 후보 검토

### 2026-04-13: `spring-petclinic/spring-framework-petclinic`

- 입력 형태:
  - 공개 GitHub 샘플 프로젝트 clone
- 실행:
  - `npm run analyze -- /tmp/spring-framework-petclinic-code2me`
- 결과:
  - `No matching profile detected`
- 판정:
  - `Pass` for boundary check
- 해석:
  - Java config/JSP 위주의 현대 Spring MVC 샘플은 현재 `legacy-java-ee` detection 경계 밖으로 남는다.

### 2026-04-13: `apache/struts-examples` `spring-struts`

- 입력 형태:
  - 공개 GitHub 샘플의 `spring-struts` 서브모듈
  - `web.xml`, `applicationContext.xml`, JSP 포함
- 실행:
  - `npm run analyze -- /tmp/struts-examples-code2me/spring-struts`
- 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 10` on 2026-04-13
  - `entryPointCount = 1`
  - `warningCount = 0`
  - 대표 non-screen route `/edit.action`, `/save.action` 복원
  - UI flow는 아직 0건
- 판정:
  - `Soft Fail`
- 실패 분류:
  - `view/layout`
- 해석:
  - 새 profile이 Struts 계열 entry와 action route는 복원한다.
  - 다만 Spring 통합 예제에서 rendered screen 쪽은 아직 충분히 연결되지 않는다.

### 2026-04-13: `apache/struts-examples` `form-validation`

- 입력 형태:
  - 공개 GitHub 샘플의 pure Struts `form-validation` 서브모듈
  - `web.xml`, `struts.xml`, JSP 포함
- 실행:
  - `npm run analyze -- /tmp/struts-examples-code2me/form-validation`
- 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 10` on 2026-04-13
  - `entryPointCount = 1`
  - 대표 non-screen route `/registerForm.action`, `/register.action`, `/hello.action` 복원
- 판정:
  - `Soft Pass`
- 해석:
  - pure Struts 계열은 새 profile로 실제 감지/복원된다.
  - 현재 결과는 route 중심이며 rendered screen 연결은 아직 약하다.

### 2026-04-13: `StripesFramework/stripes` `examples`

- 입력 형태:
  - 공개 GitHub Stripes examples 앱
- 실행:
  - `npm run analyze -- /tmp/stripes-code2me/examples`
- 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 9` on 2026-04-13
  - `entryPointCount = 1`
  - non-screen action route 다수 복원
  - screen flow는 거의 생성되지 않음
- 판정:
  - `Soft Fail`
- 해석:
  - Stripes examples는 이제 경계 밖이 아니라 새 profile로 흡수된다.
  - 그러나 action -> JSP 렌더 경로와 UI flow 복원은 아직 불충분하다.

### 2026-04-13: `spring-petclinic/spring-framework-petclinic`

- 실행:
  - `npm run analyze -- /tmp/spring-framework-petclinic-code2me`
- 결과:
  - `No matching profile detected` on 2026-04-13
- 판정:
  - `Pass` for boundary check
- 해석:
  - `action-family-legacy-web`를 추가한 뒤에도 Java config 중심 Spring MVC는 여전히 현재 경계 밖으로 남는다.

### 2026-04-13: practical read-only gate on `legacy-java-ee-minimal`

- 입력 형태:
  - 로컬 sample copy를 `chmod -R a-w`로 read-only 전환한 디렉터리
- 실행:
  - `npm run analyze -- /tmp/legacy-java-ee-minimal-readonly`
- 결과:
  - 대상 `.code2me/` 쓰기는 `EACCES`로 실패
  - 내부 미러 `/home/u24/projects/code2me/.code2me-result/projects/legacy-java-ee-minimal-readonly`는 정상 생성
  - `profileDetection.score = 12`, `entryPointCount = 1`
- 판정:
  - `Pass`
- 해석:
  - target write 실패 시 internal mirror fallback이 실제 read-only 프로젝트에서도 유지된다.


## 2026-04-13 update: action-family linkage pass

### `mybatis/jpetstore-6`

- 재실행:
  - `npm run analyze -- /tmp/jpetstore-6-code2me`
- 최신 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 9`
  - `entryPointCount = 1`
  - `renders = 15`
  - `screenFlowCards = 18`
  - `apiFlowCards = 3`
- 판정:
  - `Soft Pass`
- 해석:
  - `ForwardResolution(CONSTANT)`와 JSP alias 연결 보강 뒤 대표 Stripes screen flow가 실제로 복원된다.
  - 다만 route naming과 redirect 대상 해석은 여전히 더 다듬을 여지가 있다.

### `StripesFramework/stripes` `examples`

- 재실행:
  - `npm run analyze -- /tmp/stripes-code2me/examples`
- 최신 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 9`
  - `entryPointCount = 1`
  - `renders = 11`
  - `screenFlowCards = 13`
  - `apiFlowCards = 13`
- 판정:
  - `Soft Pass`
- 해석:
  - `src/main/webapp` JSP alias와 Stripes `beanclass`/submit parsing 보강 뒤, bugzooky/quickstart 계열 대표 screen flow가 복원된다.
  - `SingleBugActionBean -> BugListActionBean` 같은 cross-class redirect는 최소 1건 복원된다.
  - REST/action mixed sample이라 non-screen 비중도 계속 높다.

### `apache/struts-examples` `form-validation`

- 재실행:
  - `npm run analyze -- /tmp/struts-examples-code2me/form-validation`
- 최신 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 10`
  - `entryPointCount = 1`
  - `renders = 3`
  - `screenFlowCards = 3`
  - `apiFlowCards = 0`
- 판정:
  - `Pass`
- 해석:
  - 최신 implicit screen 승격 규칙까지 반영 뒤 `registerForm.action`, `register.action`, `hello.action`이 모두 screen flow로 분류된다.

### `apache/struts-examples` `spring-struts`

- 재실행:
  - `npm run analyze -- /tmp/struts-examples-code2me/spring-struts`
- 최신 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 10`
  - `entryPointCount = 1`
  - `renders = 2`
  - `screenFlowCards = 2`
  - `apiFlowCards = 0`
- 판정:
  - `Soft Pass`
- 해석:
  - 최신 JSP alias 정규화 뒤 Spring 통합 Struts 예제도 대표 screen flow 2건이 바로 복원된다.
  - 남은 보강 축은 Stripes session alias 정규화와 Struts chain wildcard 세분화다.


### 2026-04-13: action-family read-only gate on `action-family-legacy-web-stripes-minimal`

- 입력 형태:
  - 로컬 action-family sample copy를 `chmod -R a-w`로 read-only 전환한 디렉터리
- 실행:
  - `npm run analyze -- /tmp/action-family-legacy-web-stripes-minimal-readonly`
- 결과:
  - 대상 `.code2me/` 쓰기는 `EACCES`로 실패
  - 내부 미러 `/home/u24/projects/code2me/.code2me-result/projects/action-family-legacy-web-stripes-minimal-readonly`는 정상 생성
  - `profileDetection.score = 11`, `entryPointCount = 1`
- 판정:
  - `Pass`
- 해석:
  - target write 실패 시 internal mirror fallback이 `action-family-legacy-web` 프로파일에서도 유지된다.


### 2026-04-13: `apache/struts-examples` `crud`

- 입력 형태:
  - 공개 GitHub 샘플의 `crud` 서브모듈
- 실행:
  - `npm run analyze -- /tmp/struts-examples-code2me/crud`
- 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 10`
  - `entryPointCount = 1`
  - `screenFlowCards = 2`
  - `apiFlowCards = 0`
  - `redirectTargets`에서 `/index.action` 복원
- 판정:
  - `Soft Pass`
- 해석:
  - Struts `redirectAction` 계열은 최소한의 route 복원까지 동작한다.
  - wildcard action route(`*Person.action`) 세분화는 후속 보강 여지가 있다.

### 2026-04-13: `apache/struts-examples` `message-store`

- 입력 형태:
  - 공개 GitHub 샘플의 `message-store` 서브모듈
- 실행:
  - `npm run analyze -- /tmp/struts-examples-code2me/message-store`
- 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 10`
  - `entryPointCount = 1`
  - `screenFlowCards = 1`
  - `apiFlowCards = 0`
- 판정:
  - `Soft Pass`
- 해석:
  - profile detection과 기본 screen 분류는 유지된다.
  - 현재는 route-level detail보다 entry summary 쪽이 더 강하게 남는다.


### 2026-04-13: read-only gate on `mybatis/jpetstore-6` mirror

- 입력 형태:
  - `/tmp/jpetstore-6-code2me`를 read-only copy한 `/tmp/jpetstore-6-code2me-readonly`
- 실행:
  - `npm run analyze -- /tmp/jpetstore-6-code2me-readonly`
- 최신 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 9`
  - `entryPointCount = 1`
  - `targetWriteError = EACCES`
  - `internalOutputDir = /home/u24/projects/code2me/.code2me-result/projects/jpetstore-6-code2me-readonly`
- 판정:
  - `Pass`
- 해석:
  - 실제 공개 샘플 크기에서도 target write 실패 시 internal mirror fallback이 유지된다.
  - action-family profile과 read-only gate 조합은 representative real-project 축에서도 재현 가능하다.

### 2026-04-28: `mybatis/jpetstore-6` on `code2me_v0.2`

- 입력 형태:
  - 공개 GitHub 샘플 프로젝트 clone
  - `/tmp/jpetstore-6-code2me`
- 실행:
  - `NPM_CONFIG_CACHE=/tmp/.npm npm run analyze -- /tmp/jpetstore-6-code2me`
- 최신 결과:
  - `profileId = action-family-legacy-web`
  - `profileDetection.score = 9`
  - `entryPointCount = 1`
  - `nodeCount = 64`
  - `edgeCount = 279`
  - `screenFlowCards = 18`
  - `apiFlowCards = 3`
  - `flowDetails = 22`
  - `sessionHintHandlers = 4`
- session alias evidence:
  - `OrderActionBean.listOrders`: `/Order.action?listOrders`, hints `/Account.action`
  - `OrderActionBean.newOrderForm`: `/Order.action?newOrderForm`, hints `/Account.action`, `/Cart.action`
  - `OrderActionBean.newOrder`: `/Order.action?newOrder`, hints `/Cart.action`
  - `OrderActionBean.viewOrder`: `/Order.action?viewOrder`, typed alias `accountBean`, hints `/Account.action`
- redirect evidence:
  - `AccountActionBean.newAccount/editAccount/signon`: redirect target `/Catalog.action`
  - `OrderActionBean.newOrderForm`: redirect target `/Account.action`
- 판정:
  - `Soft Pass`
- 해석:
  - 0.2의 Stripes `session.getAttribute("/actions/*.action")` evidence 보존이 대표 공개 샘플에서 실제로 동작한다.
  - session alias는 새 flow classification으로 승격하지 않고 `Flow Details > Request Path` supporting evidence로 유지하는 현재 방향이 안전하다.
  - route 문자열이 아닌 `accountBean` style typed session attribute alias도 ActionBean 타입 선언/캐스팅에서 복원된다.
  - 남은 축은 Struts redirect-action / chain wildcard 세분화와 Struts 1 1차 지원이다.
